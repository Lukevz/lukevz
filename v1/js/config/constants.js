/**
 * Application Constants
 * Global configuration values used throughout the application
 */

// Default music folder order (displayed in this sequence)
export const defaultMusicFolders = ['Music', 'Podcasts', 'Ambience', 'Sounds'];

// Tags to hide from the sidebar navigation
export const hiddenTags = ['status'];

// LocalStorage key for persisting last open window
export const LAST_OPEN_WINDOW_STORAGE_KEY = 'lastOpenWindowId';

// Playlist cache duration (1 hour in milliseconds)
export const PLAYLIST_CACHE_DURATION = 3600000; // 1 hour
